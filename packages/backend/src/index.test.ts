import { describe, it } from "vitest";

describe("Routing", () => {
	it.todo("should return a 404 error if the route is not found");

	it.todo(
		"should return a 405 error if the method is not allowed for the route"
	);

	it.todo(
		"should return a 500 error if an internal server error occurs"
	);

	it.todo("should return a 200 response if the route is found");
});

describe("Golo365 Route", () => {
	it.todo("should handle a valid request to the route");
});
